{
  "name": "indexes-sum-match-finder",
  "version": "1.0.0",
  "description": "Write a function to return the indices of the two numbers in the array which add up to a given number",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.6.1"
  },
  "keywords": [
    "typescript",
    "parcel-bundler",
    " parcel",
    "recursive function"
  ]
}
